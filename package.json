{
  "name": "travel-crm",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "node backend/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "googleapis": "^130.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
